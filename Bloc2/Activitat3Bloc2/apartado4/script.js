function transformers(){
    let input_usuario = prompt("Escribe lo que quieras:")

    let lower_input = input_usuario.toLowerCase()
    let upper_input = input_usuario.toUpperCase()

    alert("Mayúscula: " + upper_input + "\n" + "Minúscula: " + lower_input)
}
